// Chapter # 26 - 30 Assignments

{ 
    // Question # 1
    // Declare a variable for store a value of user.
    // var userInput = +prompt("Add a number with decimal values");

    // document.write(`Number: ${userInput} <br>`);

    // Using Math.round() Method for round decimal values
    // document.write(`Round off value: ${Math.round(userInput)} <br>`);
    
    // Using floor method for rounds a number nearest down to the integer
    // document.write(`Floor value: ${Math.floor(userInput)} <br>`);
    
    // Using ceil method for rounds a number nearest up to the integer
    // document.write(`Ceil value: ${Math.ceil(userInput)} <br>`);
}

// {
    // Question # 2

    // Declare a variable for store a value of user.
    // var userInput = +prompt("Add a Negative number with decimal values");

    // document.write(`Number: ${userInput} <br>`);

    // Using Math.round() Method for round decimal values
    // document.write(`Round off value: ${Math.round(userInput)} <br>`);

    // Using floor method for rounds a negative number nearest up to the integer
    // document.write(`Floor value: ${Math.floor(userInput)} <br>`);

    // Using ceil method for rounds a negative number nearest down to the integer
    // document.write(`Ceil value: ${Math.ceil(userInput)} <br>`);
// }


// {
    // Question # 3
    // Make variable with Negative number and a variable with positive number
    // var value = -4;
    // var value2 = 5;

    /* Using absolute method for convert a value from negative to positive.
    abs() method can convert only negative values to positive not positive to negative */
    // document.write(`The absolute value of ${value} is ${Math.abs(value)} <br> & value of ${value2} is ${Math.abs(value2)}`);
// }


// {
    // Question # 4
    // Generating random value between 1 to 6 using Math.floor() and Math.random() Methods.

    // document.write(`Random dice value: ${Math.floor(Math.random() * 6 + 1)} <br>`);
    // document.write(`Random dice value: ${Math.floor(Math.random() * 6 + 1)}`);
// }

// Question # 5
// {
    //Declaring variable for generating random number 1 or 2.
    // var coinValue = Math.floor(Math.random() * 2 + 1)        // Math.floor() => using for round value down to nearest integer.
    // if (coinValue === 2) {
        // document.write(`${coinValue} <br> random coin value: Heads`)
    // }else {
        // document.write(`${coinValue} <br> Random coin value: Tails`)
    // }
       
// }


// Question # 6
// {
    // Generating random number between 1 and 100
    // document.write(`Random number between 1 and 100: ${Math.ceil(Math.random() * 100)}`);
// }

// Question # 7
// {
    // Declare a variable to store user weight and using parse float for convert a string value to number and also store decimal values.
    // var userWeight = parseFloat(prompt(`Enter your weight in kilograms`));
    // document.write(`The weight of user is ${userWeight.toFixed(1)} Kilograms`);    // => tofixed() method for fixed decimal values counting.
// }


// Question # 8
// {
    // Declare a variable generating number between 1 and 10
    // var secretNumber = Math.ceil(Math.random() * 10);
    // Declare another variable for take number from user
    // var userInput = +prompt(`Guess a number between 1 and 10`);

    // Check values of secretNumber and userInput using if - else condition
//     if (secretNumber === userInput) {
//         alert("Congratulation! Your guess is correct")
//     }else {
//         alert("Try again!")
//     }
// }




    /* ++++++++++++++++++++++++++++++++++++ THE END +++++++++++++++++++++++++++++++++++++++ */